import { useState, useLayoutEffect } from "react";
import {
  StyleSheet,
  Text,
  View,
  FlatList,
  TouchableOpacity,
} from "react-native";

const CustomizeList = () => {
  return (
    <View>
      <Text>Cutomize List</Text>
    </View>
  );
};

export default CustomizeList;

const styles = StyleSheet.create({});
